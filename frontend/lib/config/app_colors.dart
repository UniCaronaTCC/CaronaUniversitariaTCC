import 'package:flutter/material.dart';

class AppColors { // cria a classe pra guardar as cores principais
  static const Color primary = Color(0xFF8F16D9); // cria a cor 'primary', a principal
  static const Color background = Colors.white; // cor do fundo
  static const Color text = Colors.black; // cor dos textos
}