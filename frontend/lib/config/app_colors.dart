import 'package:flutter/material.dart'; // importa os recursos visuais do Flutter, incluindo cores

class AppColors { // classe responsável por guardar as cores principais do app
  static const Color primary = Color(0xFF8F16D9); // cor principal do app
  static const Color background = Colors.white; // cor padrão de fundo
  static const Color text = Colors.black; // cor padrão dos textos
}